'use client';
import { useState } from 'react';
import { Bot, MessageCircle, Send, X } from 'lucide-react';
export function ChatbotWidget() { const [open,setOpen]=useState(false); const [input,setInput]=useState(''); const [busy,setBusy]=useState(false); const [messages,setMessages]=useState<Array<{role:'user'|'assistant';content:string}>>([{role:'assistant',content:'Hi! I’m the ContactReachout AI assistant. How can I help with contact-form outreach, campaigns, AI personalization or pricing?'}]); const send=async()=>{const question=input.trim();if(!question||busy)return;const history=messages;setMessages([...messages,{role:'user',content:question}]);setInput('');setBusy(true);try{const r=await fetch('/api/chatbot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,history})});const d=await r.json();setMessages(current=>[...current,{role:'assistant',content:r.ok?d.answer:d.error||'Please contact hello@contactreachout.com.'}]);}catch{setMessages(current=>[...current,{role:'assistant',content:'Please contact hello@contactreachout.com for help.'}]);}finally{setBusy(false);}};return <div className="fixed bottom-5 right-5 z-50"><button onClick={()=>setOpen(!open)} aria-label="Open ContactReachout AI assistant" className="grid h-14 w-14 place-items-center rounded-full bg-[#0e6de4] text-white shadow-xl">{open?<X/>:<MessageCircle/>}</button>{open&&<div className="absolute bottom-16 right-0 flex h-[min(560px,calc(100vh-120px))] w-[min(360px,calc(100vw-2rem))] flex-col overflow-hidden rounded-3xl border border-blue-100 bg-white shadow-2xl"><div className="flex items-center gap-3 bg-[#0e6de4] p-4 text-white"><span className="grid h-9 w-9 place-items-center rounded-xl bg-white/20"><Bot className="h-5 w-5"/></span><div><p className="font-black">ContactReachout AI</p><p className="text-xs text-blue-100">Support assistant</p></div></div><div className="flex-1 space-y-3 overflow-y-auto p-4">{messages.map((message,index)=><div key={index} className={`max-w-[85%] rounded-2xl px-3 py-2 text-sm leading-6 ${message.role==='user'?'ml-auto bg-[#0e6de4] text-white':'bg-blue-50 text-slate-700'}`}>{message.content}</div>)}{busy&&<div className="rounded-2xl bg-blue-50 px-3 py-2 text-sm text-slate-500">Typing…</div>}</div><div className="flex gap-2 border-t border-slate-100 p-3"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Ask a question…" className="min-w-0 flex-1 rounded-xl border border-blue-100 px-3 py-2 text-sm outline-none focus:border-[#0e6de4]"/><button onClick={send} disabled={busy||!input.trim()} className="grid h-10 w-10 place-items-center rounded-xl bg-[#0e6de4] text-white disabled:opacity-50"><Send className="h-4 w-4"/></button></div></div>}</div> }
