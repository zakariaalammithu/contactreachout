'use client';
import { useEffect, useState } from 'react';
import { Bot, Save } from 'lucide-react';
import { Button } from '@/components/ui/Button';
export default function AdminChatbotPage(){const [prompt,setPrompt]=useState('');const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);useEffect(()=>{fetch('/api/admin/chatbot').then(r=>r.json()).then(d=>setPrompt(d.prompt||d.defaultPrompt||''));},[]);const save=async()=>{setBusy(true);setStatus('');const r=await fetch('/api/admin/chatbot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt})});const d=await r.json();setStatus(r.ok?d.message:d.error||'Could not save.');setBusy(false);};return <div className="max-w-4xl space-y-6"><div><h1 className="flex items-center gap-2 text-2xl font-black text-slate-950"><Bot className="h-6 w-6 text-[#0e6de4]"/>AI Chatbot Knowledge & Prompt</h1><p className="mt-1 text-sm text-slate-500">Manage the instructions used by the public ContactReachout support assistant.</p></div><div className="rounded-3xl border border-blue-100 bg-white p-6 shadow-sm"><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} rows={24} className="w-full rounded-2xl border border-blue-100 p-4 font-mono text-sm leading-6 outline-none focus:border-[#0e6de4]" placeholder="Add detailed product, pricing, FAQ and support guidance…"/><div className="mt-4 flex items-center justify-between gap-3"><span className="text-xs text-slate-500">{prompt.length.toLocaleString()} characters · server-side only</span><Button onClick={save} disabled={busy||prompt.trim().length<80}><Save className="mr-2 h-4 w-4"/>Save instructions</Button></div>{status&&<p className="mt-4 rounded-xl bg-blue-50 p-3 text-sm font-semibold text-blue-800">{status}</p>}</div></div>}
