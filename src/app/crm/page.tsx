import LeadsPage from '@/app/leads/page';

// Unified CRM entry point. The existing Contact List implementation is the
// shared lead/list workspace used by both user and admin navigation.
export { dynamic } from '@/app/leads/page';

export default LeadsPage;
